<p id="notice"><%#= notice %></p>

<h1><%#= link_to "個別契約基本情報一覧", root_path %>  正社員・パート社員一覧</h1>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>基本情報</th>
      <th>振込口座</th>
      <th>緊急連絡先</th>
      <th>扶養情報</th>
      <th>最終学歴</th>
      <th>最終就労先</th>
      <th>各種資格　取得年月</th>
      <th>学生確認</th>
      <th>会社記入欄</th>
      <th>雇用契約期間（西暦）</th>
      <th>在留カード情報</th>
      <th>学生証情報</th>
      <th>労使協定</th>
      <th>36協定</th>
      <th>社会保険</th>                  
    </tr>
  </thead>

  <tbody style="text-align: center;">
    <% @employee_infos.each do |ei| %>
      <tr>

        <td><%= link_to "#{ei.first_name}", employee_info_path(ei.id) %>

        <!-- 振込口座 -->
        <% if ei.bank_account.nil? %>
          <td><%= link_to "作成", new_employee_info_bank_account_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_bank_account_path(ei.id, ei.bank_account.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
       <!--  緊急連絡先 -->
        <% if ei.contact.nil? %>
          <td><%= link_to "作成", new_employee_info_contact_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_contact_path(ei.id, ei.contact.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
        <!-- 扶養情報 -->
        <% if ei.family.nil? %>
          <td><%= link_to "作成", new_employee_info_family_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_family_path(ei.id, ei.family.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
        <!-- 最終学歴 -->
        <% if ei.education.nil? %>
          <td><%= link_to "作成", new_employee_info_education_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_education_path(ei.id, ei.education.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
        <!-- 最終就労先 -->
        <% if ei.work_experience.nil? %>
          <td><%= link_to "作成", new_employee_info_work_experience_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_work_experience_path(ei.id, ei.work_experience.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
        <!-- 各種資格　取得年月 -->
        <% if ei.qualifications.nil? %>
          <td><%= link_to "作成", new_employee_info_qualification_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%#= link_to "見る", employee_info_qualification_path(ei.id, ei.qualifications.first.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
        <!-- 学生確認 -->
        <% if ei.student_check.nil? %>
          <td><%= link_to "作成", new_employee_info_student_check_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_student_check_path(ei.id, ei.student_check.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>

        <!--  会社記入欄 -->
        <% if ei.work_info.nil? %>
          <td><%= link_to "作成", new_employee_info_work_info_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_work_info_path(ei.id, ei.work_info.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>        

        <!-- 雇用契約期間（西暦） -->
        <% if ei.employment_period.nil? %>
          <td><%= link_to "作成", new_employee_info_employment_period_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_employment_period_path(ei.id, ei.employment_period.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
        <!--   在留カード情報 -->
        <% if ei.residence_card.nil? %>
          <td><%= link_to "作成", new_employee_info_residence_card_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_residence_card_path(ei.id, ei.employment_period.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
        <!-- 学生証情報 -->
        <% if ei.student_card.nil? %>
          <td><%= link_to "作成", new_employee_info_student_card_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_student_card_path(ei.id, ei.student_card.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
        <!-- 労使協定 -->
        <% if ei.labor_management_agreement.nil? %>
          <td><%= link_to "作成", new_employee_info_labor_management_agreement_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_labor_management_agreement_path(ei.id, ei.labor_management_agreement.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
        <!--  36協定 -->
        <% if ei.labor_standards_act.nil? %>
          <td><%= link_to "作成", new_employee_info_labor_standards_act_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_labor_standards_act_path(ei.id, ei.labor_standards_act.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
        <!-- 社会保険 -->
        <% if ei.social_insurance.nil? %>
          <td><%= link_to "作成", new_employee_info_social_insurance_path(ei.id), class: "btn btn-outline-secondary btn-block" %>
          </td>
        <% else %>
          <td>
            <%= link_to "見る", employee_info_social_insurance_path(ei.id, ei.social_insurance.id), class: "btn btn-outline-info btn-block" %>
          </td>
        <% end %>
                
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新規作成', new_employee_info_path, class: "btn btn-primary btn-block" %>
<p id="notice"><%#= notice %></p>



















