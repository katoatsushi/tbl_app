<h1 style="text-align: center;">緊急連絡先</h1>
<%= form_for @contact , url:  employee_info_contacts_path(params[:employee_info_id]) do |f| %>

		氏名:   <%= f.text_field :name %>	
		フリガナ:	<%= f.text_field :name_kana %>
		続柄:	<%= f.text_field :who_is_this %>
		住　所:	<%= f.text_field :address %>
		緊急連絡先:	<%= f.text_field :tel %>

	<%=f.submit '作成', class: 'btn btn-primary btn-lg btn-block' %>
<%end%>

